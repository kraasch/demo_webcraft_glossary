import { RibbonFullNamesPipe } from './ribbon-full-names.pipe';

describe('RibbonFullNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new RibbonFullNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
